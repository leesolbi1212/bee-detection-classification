import React from "react";
import ReactDOM from "react-dom/client";
import BeeDetectorApp from "./BeeDetectorApp.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BeeDetectorApp />
  </React.StrictMode>
);
